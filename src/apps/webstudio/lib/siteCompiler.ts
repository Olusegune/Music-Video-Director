import { patternById } from "@/apps/webstudio/lib/patterns";
import type { DesignTokens, SectionInstance, WebProject } from "@/apps/webstudio/lib/types";

const esc = (value: string) => value.replace(/[&<>"']/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#039;" })[character] ?? character);
const list = (items: string[], className = "cards") => `<div class="${className}">${items.map((item) => `<article><span class="mark" aria-hidden="true"></span><p>${esc(item)}</p></article>`).join("")}</div>`;

function sectionHtml(section: SectionInstance, primaryHeading = false) {
  const copy = section.copy;
  const pattern = patternById(section.patternId);
  const headingTag = primaryHeading ? "h1" : "h2";
  const intro = `<p class="eyebrow">${esc(copy.eyebrow)}</p><${headingTag}>${esc(copy.heading)}</${headingTag}><p class="lede">${esc(copy.body)}</p>`;
  const heroIntro = intro;
  const cta = copy.ctaLabel ? `<a class="button" href="#contact">${esc(copy.ctaLabel)}</a>` : "";
  switch (pattern.id) {
    case "hero-split": return `<section class="hero split"><div>${heroIntro}${cta}</div><div class="visual" role="img" aria-label="Brand campaign image">${section.mediaUrl ? `<img src="${esc(section.mediaUrl)}" alt="${esc(copy.heading)}">` : `<span>${esc(copy.items[0] ?? "Your story, art directed")}</span>`}</div></section>`;
    case "hero-centered": return `<section class="hero centered">${heroIntro}${cta}<div class="proofline">${copy.items.map(esc).join(" · ")}</div></section>`;
    case "hero-editorial": return `<section class="hero editorial"><p class="eyebrow">${esc(copy.eyebrow)}</p><${headingTag}>${esc(copy.heading)}</${headingTag}><div class="editorial-row"><p>${esc(copy.body)}</p>${cta}</div></section>`;
    case "feature-alternating": return `<section>${intro}<ol class="steps">${copy.items.map((item, index) => `<li><b>${String(index + 1).padStart(2, "0")}</b><span>${esc(item)}</span></li>`).join("")}</ol></section>`;
    case "stats-band": return `<section class="band">${intro}${list(copy.items, "stats")}</section>`;
    case "testimonials-grid": return `<section>${intro}<div class="quotes">${copy.items.map((item) => `<blockquote>“${esc(item)}”</blockquote>`).join("")}</div></section>`;
    case "logo-cloud": return `<section class="compact">${intro}<div class="logos">${copy.items.map((item) => `<span>${esc(item)}</span>`).join("")}</div></section>`;
    case "case-study": return `<section class="case">${intro}<aside>${list(copy.items)}</aside></section>`;
    case "pricing-simple": return `<section class="offer">${intro}<div class="offer-card">${list(copy.items)}${cta}</div></section>`;
    case "faq-stack": return `<section>${intro}<div class="faq">${copy.items.map((item) => `<details><summary>${esc(item)}</summary><p>${esc(copy.body)}</p></details>`).join("")}</div></section>`;
    case "cta-banner": return `<section id="contact" class="cta">${intro}${cta}</section>`;
    default: return `<section>${intro}${list(copy.items)}${cta}</section>`;
  }
}

export function compileCss(tokens: DesignTokens) {
  return `:root{--bg:${tokens.background};--surface:${tokens.surface};--text:${tokens.text};--muted:${tokens.muted};--primary:${tokens.primary};--accent:${tokens.accent};--radius:${tokens.radius}px;--max:${tokens.maxWidth}px}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:var(--bg);color:var(--text);font-family:${tokens.fontBody};line-height:1.6}a{color:inherit}header{position:sticky;top:0;z-index:5;display:flex;justify-content:space-between;align-items:center;padding:18px max(24px,calc((100vw - var(--max))/2));background:color-mix(in srgb,var(--bg) 88%,transparent);backdrop-filter:blur(16px);border-bottom:1px solid color-mix(in srgb,var(--text) 12%,transparent)}header strong{font-family:${tokens.fontDisplay};font-size:1.2rem}main{overflow:hidden}section{max-width:var(--max);margin:auto;padding:96px 32px}.hero{min-height:76vh;display:grid;align-items:center}.split{grid-template-columns:1.08fr .92fr;gap:64px}.centered{text-align:center;justify-items:center;max-width:900px}.editorial h1,.hero h1{font-size:clamp(3.3rem,8vw,7.8rem);line-height:.94;letter-spacing:-.055em;margin:.15em 0;font-family:${tokens.fontDisplay}}h2{font:700 clamp(2.2rem,5vw,4.5rem)/1.02 ${tokens.fontDisplay};letter-spacing:-.04em;margin:.2em 0}.eyebrow{text-transform:uppercase;letter-spacing:.18em;color:var(--accent);font-size:.75rem;font-weight:800}.lede{max-width:700px;color:var(--muted);font-size:1.1rem}.button{display:inline-block;margin-top:22px;padding:13px 20px;border-radius:999px;background:var(--primary);color:white;text-decoration:none;font-weight:800}.visual{min-height:480px;border-radius:calc(var(--radius)*1.4);background:linear-gradient(135deg,var(--primary),var(--accent));display:flex;align-items:flex-end;padding:28px;overflow:hidden;font:700 1.4rem ${tokens.fontDisplay};box-shadow:0 30px 80px color-mix(in srgb,var(--primary) 30%,transparent)}.visual img{width:100%;height:480px;object-fit:cover;margin:-28px}.cards,.quotes,.stats{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;margin-top:36px}.cards article,.quotes blockquote,.offer-card,.faq details{margin:0;padding:24px;border-radius:var(--radius);background:var(--surface);border:1px solid color-mix(in srgb,var(--text) 10%,transparent)}.mark{display:block;width:34px;height:4px;background:var(--accent);border-radius:9px}.steps{list-style:none;padding:0}.steps li{display:grid;grid-template-columns:80px 1fr;padding:26px 0;border-bottom:1px solid color-mix(in srgb,var(--text) 14%,transparent);font-size:1.2rem}.steps b{color:var(--accent)}.band,.cta{max-width:none;background:var(--surface);padding-left:max(32px,calc((100vw - var(--max))/2));padding-right:max(32px,calc((100vw - var(--max))/2))}.stats article{text-align:center;font:700 1.2rem ${tokens.fontDisplay}}.logos{display:flex;flex-wrap:wrap;gap:12px;margin-top:28px}.logos span{padding:10px 16px;border:1px solid color-mix(in srgb,var(--text) 16%,transparent);border-radius:999px}.case{display:grid;grid-template-columns:1fr 1fr;gap:40px}.offer-card{max-width:650px;margin-top:32px}.faq{display:grid;gap:10px;margin-top:32px}.faq summary{cursor:pointer;font-weight:800}.proofline{margin-top:40px;color:var(--muted)}footer{padding:40px 32px;text-align:center;color:var(--muted);border-top:1px solid color-mix(in srgb,var(--text) 10%,transparent)}@media(max-width:760px){section{padding:68px 22px}.split,.case{grid-template-columns:1fr}.hero{min-height:auto}.visual{min-height:320px}.visual img{height:320px}.cards,.quotes,.stats{grid-template-columns:1fr}.editorial h1,.hero h1{font-size:clamp(2.8rem,16vw,5rem)}header{padding:15px 20px}}@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}`;
}

export function compileSite(project: WebProject, inlineCss = true) {
  const css = compileCss(project.tokens);
  const sections = project.sections.map((section, index) => sectionHtml(section, index === 0)).join("\n");
  const description = project.positioning.promise || project.businessDescription;
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="${esc(description)}"><meta name="theme-color" content="${esc(project.tokens.background)}"><meta property="og:title" content="${esc(project.businessName)} — ${esc(project.positioning.offer)}"><meta property="og:description" content="${esc(description)}"><meta property="og:type" content="website"><title>${esc(project.businessName)} — ${esc(project.positioning.offer)}</title>${inlineCss ? `<style>${css}</style>` : `<link rel="stylesheet" href="styles.css">`}</head><body><header><strong>${esc(project.businessName)}</strong><nav aria-label="Primary navigation"><a href="#contact">${esc(project.positioning.cta)}</a></nav></header><main>${sections}</main><footer>© ${new Date().getFullYear()} ${esc(project.businessName)}. Built with Director Studio.</footer></body></html>`;
}
